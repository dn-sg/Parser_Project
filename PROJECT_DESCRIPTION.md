# Описание проекта Parser Project

## Обзор

**Parser Project** — это система для автоматизированного парсинга финансовых данных и новостей из различных источников. Проект собирает информацию об акциях, дивидендах и новостях, сохраняет её в базу данных и предоставляет удобный веб-интерфейс для просмотра и анализа данных.

## Основные возможности

### 📊 Парсеры данных

Проект включает три специализированных парсера:

1. **RBC News Parser** (`src/parsers/sources/rbc.py`)
   - Парсит новости с сайта РБК (rbc.ru)
   - Извлекает заголовки и полный текст статей
   - Находит ссылки на новости на главной странице
   - Переходит по каждой ссылке и извлекает контент
   - Фильтрует рекламу и служебную информацию

2. **SmartLab Stocks Parser** (`src/parsers/sources/smartlab.py`)
   - Парсит данные об акциях с сайта SmartLab (smart-lab.ru)
   - Извлекает информацию о компаниях: название, тикер, цена, изменение цены
   - Собирает данные об объеме торгов и капитализации
   - Обрабатывает числовые данные (цены, проценты)

3. **Dohod Dividends Parser** (`src/parsers/sources/dohod.py`)
   - Парсит информацию о дивидендах с сайта Dohod.ru
   - Извлекает данные о компаниях, тикерах, секторах
   - Собирает информацию о выплатах дивидендов
   - Обрабатывает даты и финансовые показатели

### 🏗️ Архитектура проекта

Проект следует принципам чистой архитектуры (D32) и разделен на слои:

```
src/
├── core/           # Ядро приложения (конфигурация)
├── api/            # REST API (FastAPI)
├── web/            # Веб-интерфейс (Streamlit)
├── parsers/        # Парсеры данных
│   └── sources/    # Конкретные реализации парсеров
├── tasks/          # Фоновые задачи (Celery)
├── services/       # Бизнес-логика
├── database/       # Модели и схемы БД
└── utils/          # Вспомогательные утилиты
```

### 🗄️ База данных

**PostgreSQL** используется для хранения данных:

- **source** — таблица источников данных
- **logs** — логирование выполнения парсеров
- **rbc_news** — новости с РБК
- **smartlab_stocks** — данные об акциях
- **dohod_divs** — информация о дивидендах

### 🔄 Асинхронная обработка

**Celery** используется для фоновой обработки задач парсинга:

- Задачи выполняются асинхронно
- Логирование всех операций
- Обработка ошибок
- Мониторинг через Flower

### 🌐 Веб-интерфейс

**Streamlit Dashboard** предоставляет:

- **Dash** — главный дашборд со статистикой и запуском парсеров
- **Logs** — история выполнения парсеров с фильтрацией
- **Dohod Divs** — просмотр данных о дивидендах
- **RBC News** — просмотр новостей с возможностью чтения полного текста
- **Smartlab Stocks** — просмотр акций с графиками цен

### 🔌 REST API

**FastAPI** предоставляет RESTful API:

- `POST /api/run/{source}` — запуск парсера (smartlab/rbc/dohod)
- `GET /api/task/{task_id}` — статус задачи
- `GET /api/stats` — статистика по данным
- `GET /api/data/{source}` — получение данных
- `GET /api/logs` — получение логов
- `GET /api/status` — статус парсеров

## Технологический стек

### Backend
- **Python 3.11+**
- **FastAPI** — веб-фреймворк для API
- **Celery** — распределенная очередь задач
- **Redis** — брокер сообщений для Celery
- **PostgreSQL** — реляционная база данных
- **SQLAlchemy** — ORM (подготовлено для миграции)
- **asyncpg** — асинхронный драйвер PostgreSQL

### Frontend
- **Streamlit** — веб-интерфейс для дашбордов
- **Pandas** — обработка данных
- **Plotly** — визуализация графиков

### Парсинг
- **BeautifulSoup4** — парсинг HTML
- **Requests** — HTTP-запросы

### Инфраструктура
- **Docker** — контейнеризация
- **Docker Compose** — оркестрация сервисов
- **PostgreSQL 15** — база данных
- **Redis 7** — кэш и брокер

### Тестирование
- **pytest** — фреймворк для тестирования
- **pytest-cov** — покрытие кода тестами
- **coverage** — анализ покрытия

## Структура проекта

```
Parser_Project/
├── src/                    # Исходный код
│   ├── core/              # Конфигурация (pydantic-settings)
│   ├── api/               # FastAPI приложение
│   ├── web/               # Streamlit дашборды
│   ├── parsers/sources/   # Парсеры (RBC, SmartLab, Dohod)
│   ├── tasks/             # Celery задачи
│   ├── database/          # Схемы БД
│   └── utils/             # Утилиты
├── infra/docker/          # Dockerfile'ы для сервисов
├── tests/                 # Тесты
├── docs/                  # Документация
├── scripts/               # Вспомогательные скрипты
├── docker-compose.yml     # Конфигурация Docker Compose
├── requirements.txt       # Python зависимости
├── pyproject.toml         # Конфигурация проекта
└── Makefile              # Команды для управления
```

## Сервисы

Проект состоит из следующих сервисов:

1. **scraper_db** — PostgreSQL база данных
2. **scraper_redis** — Redis для Celery
3. **scraper_web** — FastAPI сервер (порт 8000)
4. **scraper_celery_worker** — Celery worker для выполнения задач
5. **scraper_flower** — Flower для мониторинга Celery (порт 5555)
6. **scraper_dashboard** — Streamlit дашборд (порт 8501)

## Запуск проекта

### Требования
- Docker и Docker Compose
- Python 3.11+ (для локальной разработки)

### Быстрый старт

1. Клонируйте репозиторий
2. Создайте файл `.env` на основе `.env.example`
3. Запустите проект:
   ```bash
   docker compose up -d --build
   ```

### Доступ к сервисам

- **API документация**: http://localhost:8000/docs
- **Streamlit Dashboard**: http://localhost:8501
- **Flower (Celery)**: http://localhost:5555

## Использование

### Запуск парсеров

Через API:
```bash
curl -X POST http://localhost:8000/api/run/rbc
curl -X POST http://localhost:8000/api/run/smartlab
curl -X POST http://localhost:8000/api/run/dohod
```

Через веб-интерфейс:
- Откройте Dashboard (http://localhost:8501)
- Перейдите в раздел "Dash"
- Нажмите кнопку запуска нужного парсера

### Просмотр данных

- **Новости**: раздел "RBC News" в Dashboard
- **Акции**: раздел "Smartlab Stocks" с графиками
- **Дивиденды**: раздел "Dohod Divs"
- **Логи**: раздел "Logs" для отслеживания выполнения

## Тестирование

Запуск тестов:
```bash
make test              # Все тесты
make test-cov         # С coverage отчетом
pytest tests/ -v      # Напрямую через pytest
```

Покрытие тестами: **89%+** (требование: не менее 65%)

## Особенности реализации

### Принципы проектирования

- **SOLID** — соблюдение принципов объектно-ориентированного проектирования
- **DRY** — отсутствие дублирования кода
- **Separation of Concerns** — разделение ответственности по слоям
- **Dependency Injection** — использование зависимостей через интерфейсы

### Обработка ошибок

- Логирование всех операций
- Обработка исключений в парсерах
- Откат транзакций при ошибках БД
- Статусы задач в Celery

### Производительность

- Асинхронная обработка задач через Celery
- Подготовка к миграции на async/await для БД
- Индексы в БД для быстрого поиска
- Кэширование через Redis

## Разработка

### Структура кода

- Все парсеры наследуются от `BaseParser`
- Единый интерфейс для всех парсеров
- Модульная структура для легкого добавления новых парсеров
- Тесты для каждого компонента

### Добавление нового парсера

1. Создайте класс парсера в `src/parsers/sources/`
2. Наследуйтесь от `BaseParser`
3. Реализуйте методы `parse()` и `save_to_db()`
4. Добавьте задачу Celery в `src/tasks/parser_tasks.py`
5. Добавьте endpoint в API
6. Создайте страницу в Streamlit Dashboard
7. Напишите тесты

## Лицензия

Проект разработан для образовательных целей.

## Контакты

По вопросам проекта обращайтесь к разработчику.

